program count
dictionary
    i, n, m : integer  // deklarasi variabel
algorithm
    read n, m
    for i := n to m do
        print i, i >> 1, i << 1, i*0.5
    endfor
    print(n, m, (n*m) >> 1)
endprogram
